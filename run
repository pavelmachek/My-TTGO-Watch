PATH=$PATH:~/.local/bin
if [ .$1 == .hw ]; then
    sudo chown pavel /dev/ttyUSB0 
    nice platformio run -t upload -e t-watch2020-v1
else
    nice platformio run && .pio/build/emulator_twatch2020/program
fi
# platformio run -t cleanall
# nice platformio run -t clean